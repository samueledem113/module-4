// Array of names
var speakWord = "Good Bye";
function speak(name) {
  console.log(speakWord + " " + name);
}
var speakWord = "Hello";

function speak(name) {
  console.log(speakWord + " " + name);
}
